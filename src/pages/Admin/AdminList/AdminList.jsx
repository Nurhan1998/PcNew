import React from 'react';
import ProductList from '../../Products/ProductList';

const List = () => {
    return (
        <div>
            <ProductList/>
        </div>
    );
};

export default List;